package Celda;

public class CeldaString extends Celda<String> {

    public CeldaString(String value) {
        super(value);
    }

    // Puedes agregar métodos específicos para String si lo necesitas
    public String getValue() {
        return super.getValue();
    }
}