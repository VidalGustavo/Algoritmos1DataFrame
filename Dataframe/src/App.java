import DataFrame.DataFrame;

import java.util.ArrayList;

public static void main(String[] args) throws Exception {
        // Usar para probar pero tratar de dejar vacio hasta que hagamos tests
        // o metamos alguna estructura basica para simplificar pruebas.
    //DataFrame iris = DataFrame.leerCSV("Dataframe/src/IRIS.csv", true);
    //DataFrame prueba = iris.randomSample();
    //System.out.println(iris.getCelda(1,0).getValue());
    //prueba.display();
//    ArrayList<Integer> columns = new ArrayList<>();
//    ArrayList<Integer> filas = new ArrayList<>();
//    filas.add(4);
//    filas.add(0);
//    iris.select(filas, columns);
}

